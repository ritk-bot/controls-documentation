Today's goal is to make a launch file for our moveit so that we can launch move_it without using demo.py which is frankly shit.
To restore a certain file to the version on github
```
git restore <path_to_file>
```
and to check the differences of the current file from the last pushed commit
```
git diff <path_to_file>
```

Apparently gz sim uses some sort of bridge to convey its clock to ros which was not present in my sim.launch.py.

```
Node(

package="ros_gz_bridge",

executable="parameter_bridge",

arguments=[

"/clock@rosgraph_msgs/msg/Clock[gz.msgs.Clock"

],

output="screen",

),
```

The biggest pointer for this is checking /joint_states:

```
ros2 topic echo /joint_states --once
```

and checking /clock and seeing whether it has any publishers:

```
ros2 topic info /clock
```


YAMLs are case sensitive pls check even small spaces like 
```
-gripper 
```
rather than 
```
- gripper
```
can cause errors.


Works manually now. Let's make the launch file at 3AM. i could be working on something else you know. The best and worst mistake of my life is joining Kratos.

![](../../Assets/Pasted%20image%2020260617024336.png)

The nerds can understand that it worked just by the ss. Also before the file you had to manually set sim time usage for robot_state_publisher and move_group.


Never mind, I did not add the use_sim_time commands in the launch because that would require modifying generic launch files generated by move_it so its no fun sadly. So every time you use the gazebo_moveit.launch.py the obv next step after it opening up is:

```
ros2 param set /robot_state_publisher use_sim_time true
ros2 param set /move_group use_sim_time true
```